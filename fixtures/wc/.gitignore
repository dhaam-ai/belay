# Regenerated byte-for-byte by wc_test.go on every run (see README.md's
# "Determinism" section) — never commit the large generated fixture itself.
/testdata/generated/

# Local build output if a developer builds the CLI by hand while working
# on the implementation (e.g. `go build -o wc .`).
/wc
/wc.exe
